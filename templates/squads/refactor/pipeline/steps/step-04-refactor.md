# Step 04 — Refactor em Passos Pequenos (refactorer)

The refactorer executes the approved plan one named move at a time, running the suite after
each step and committing one logical step per commit (`refactor: <move> in <target>`). A red
suite reverts the last step rather than patching forward. Produces `output/refactor-report.md`.
