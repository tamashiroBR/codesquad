# Step 04 — Design (agent: architect)

Dispatch **Artur Arquiteto** with `tasks/design-change.md`. Input: approved `output/spec.md`
and `data/repo-conventions.md`. Output: `output/design.md` — modules, dependency direction,
interface contracts, ADRs, blast radius, riskiest assumption.
