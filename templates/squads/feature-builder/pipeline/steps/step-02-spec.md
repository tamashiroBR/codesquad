# Step 02 — Specification (agent: spec-writer)

Dispatch **Sofia Spec** with `tasks/write-spec.md`. Input: `output/intake.md` and the repo
context. Output: `output/spec.md` with testable acceptance criteria, edge cases, out-of-scope,
and any blocking open questions.
