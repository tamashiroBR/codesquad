# Step 02 — Contexto e Triagem (triager)

The triager fetches the diff, description, and linked issue; states the declared intent; flags
scope creep and Definition-of-Done gaps (missing tests/description/breaking-change note);
classifies risk signals; and records suite status. Produces `output/pr-context.md`.
