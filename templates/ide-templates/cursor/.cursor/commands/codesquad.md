---
description: Codesquad — Multi-agent orchestration framework. Create and run AI squads.
---

Activate the Codesquad system. Read `.cursor/rules/codesquad.mdc` for full instructions, then follow the Initialization and Command Routing steps defined there.

If this command was invoked with arguments (e.g., `/codesquad create ...`, `/codesquad run ...`), route to the matching action from the Command Routing table.

If invoked without arguments, show the Main Menu.
